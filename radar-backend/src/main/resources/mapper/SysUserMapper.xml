<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hotchpotch.radarbackend.domain.mapper.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.hotchpotch.radarbackend.domain.entity.SysUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="email" property="email"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar_path" property="avatarPath"/>
        <result column="status" property="status"/>
        <result column="role" property="role"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="BaseColumns">
        id,
        username,
        password_hash,
        email,
        nickname,
        avatar_path,
        status,
        role,
        last_login_time,
        create_time,
        update_time
    </sql>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM sys_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT
        <include refid="BaseColumns"/>
        FROM sys_user
        WHERE email = #{email}
        LIMIT 1
    </select>

    <select id="countEnabledNormalUsers" resultType="long">
        SELECT COUNT(*)
        FROM sys_user
        WHERE status = 1
          AND role = 'USER'
    </select>

</mapper>
